import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    marginTop: 50,
    justifyContent: 'center',
    alignItems: 'center',
    gap: 20,
  },
  content: {
    color: '#808080',
    fontSize: 14,
    fontWeight: '600',
  },
  subContent: {
    color: '#808080',
    fontSize: 14,
  },
})
