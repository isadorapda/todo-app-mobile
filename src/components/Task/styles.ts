import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#262626',
    borderRadius: 8,
    flexDirection: 'row',
    padding: 12,
    marginHorizontal: 24,
    gap: 8,
  },
  taskTitle: {
    color: '#F2F2F2',
    fontSize: 14,
    flex: 1,
  },
})
